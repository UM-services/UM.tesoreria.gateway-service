title: UM Tesorería Gateway Service
description: Servicio Gateway para el enrutamiento y balanceo de carga de los Microservicios de UM Tesorería
theme: jekyll-theme-cayman

# Configuración importante para Jekyll
defaults:
  -
    scope:
      path: "" # aplica a todos los archivos
    values:
      layout: default

# Especificar que los archivos .md deben ser procesados
markdown: kramdown
kramdown:
  input: GFM
  syntax_highlighter: rouge

# Configurar la página principal
index_page: index.md 